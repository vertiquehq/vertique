<?xml version="1.0" encoding="UTF-8"?>
<!--
SPDX-FileCopyrightText: 2026 Koivisto Capital Oy
SPDX-License-Identifier: EUPL-1.2
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.vertique</groupId>
        <artifactId>vertique-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>vertique-json-schema</artifactId>
    <name>Vertique JSON Schema</name>
    <description>Generates deterministic annotation-driven JSON Schemas from resolved Java types.</description>

    <dependencies>
        <dependency>
            <groupId>dev.vertique</groupId>
            <artifactId>vertique-core</artifactId>
        </dependency>

        <!-- Annotation sources victools introspects -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
        </dependency>

        <!-- victools: annotation -> JSON Schema synthesis -->
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-generator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-module-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-module-jakarta-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-module-swagger-2</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
          Test scope only: the proof tests exercise the real built-in `vertique-strict` profile and
          its declared BigDecimal schema override, obtained through DefaultJsonMapperProfileRegistry.
          This module's production code depends on vertique-core alone for the profile contracts.
        -->
        <dependency>
            <groupId>dev.vertique</groupId>
            <artifactId>vertique-json</artifactId>
            <scope>test</scope>
        </dependency>
        <!--
          Test scope only: SchemaFixtureMatrixTest compiles a generated schema through the real
          vertx-json-schema validator to pin its pattern-matching semantics (find vs. match) against
          the anchored strict-decimal pattern (S6, PRD §6.2/§2 finding 12). Production code never
          depends on a validator — a generated schema document is transport-neutral.
        -->
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-json-schema</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
