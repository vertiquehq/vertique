<?xml version="1.0" encoding="UTF-8"?>
<!--
SPDX-FileCopyrightText: 2026 Koivisto Capital Oy
SPDX-License-Identifier: EUPL-1.2
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.vertique</groupId>
        <artifactId>codegen-it-jaxrs-applications</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>codegen-it-jaxrs-applications-app</artifactId>

    <!--
    Applications-only compilation unit (T003 TP-005). At the baseline processor this unit has no
    DI-eligible resource of its own, so no module is written for it: AppComponent's reference to its
    own dev.vertique.it.apps.app.GeneratedJaxRsResourcesModule fails to compile, the intended red.
    -->
    <dependencies>
        <dependency>
            <groupId>dev.vertique</groupId>
            <artifactId>codegen-it-jaxrs-applications-resources</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.vertique</groupId>
            <artifactId>vertique-rest-jaxrs</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-web-client</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
