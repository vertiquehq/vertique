<?xml version="1.0" encoding="UTF-8"?>
<!--
SPDX-FileCopyrightText: 2026 Koivisto Capital Oy
SPDX-License-Identifier: EUPL-1.2
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.vertique</groupId>
        <artifactId>codegen-it-jaxrs-applications</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>codegen-it-jaxrs-applications-legacy</artifactId>

    <!--
    T003 TP-006: a zero-declaration consumer of the `resources` unit. Declares no Application, so the
    real processor never scans one here; @JaxRsResources must keep serving the default mount, exactly
    as it did before T003's application-registration emission existed (I-1).
    -->
    <dependencies>
        <dependency>
            <groupId>dev.vertique</groupId>
            <artifactId>codegen-it-jaxrs-applications-resources</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.vertique</groupId>
            <artifactId>vertique-rest-jaxrs</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
