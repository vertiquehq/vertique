<?xml version="1.0" encoding="UTF-8"?>
<!--
SPDX-FileCopyrightText: 2026 Koivisto Capital Oy
SPDX-License-Identifier: EUPL-1.2
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!--
      Standalone parent for third-party Vertique applications. It is published
      exactly as authored: a literal version, the BOM import, the compiler
      wiring an application needs, and nothing of the framework's own build or
      release machinery (no CI-friendly version placeholder, no flatten plugin,
      no formatter).
      The framework's release tooling applies a final version to this file in
      its disposable release workspace; the post-release bump keeps the literal
      in step with the root <revision>, and publication-contract.test.mjs holds
      the two equal.
    -->
    <groupId>dev.vertique</groupId>
    <artifactId>vertique-app-parent</artifactId>
    <version>0.3.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Vertique Application Parent</name>
    <description>Standalone Maven parent for Vertique applications</description>
    <url>https://vertique.dev</url>

    <licenses>
        <license>
            <name>European Union Public Licence v. 1.2</name>
            <url>https://joinup.ec.europa.eu/collection/eupl/eupl-text-eupl-12</url>
        </license>
    </licenses>

    <organization>
        <name>Koivisto Capital Oy</name>
        <url>https://vertique.dev</url>
    </organization>

    <developers>
        <developer>
            <id>vertique-release</id>
            <name>Vertique Release</name>
            <email>releases@vertique.dev</email>
            <organization>Koivisto Capital Oy</organization>
            <organizationUrl>https://vertique.dev</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/vertiquehq/vertique.git</connection>
        <developerConnection>scm:git:git@github.com:vertiquehq/vertique.git</developerConnection>
        <url>https://github.com/vertiquehq/vertique</url>
    </scm>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.vertique</groupId>
                <artifactId>vertique-bom</artifactId>
                <!-- Literal on purpose: a project-version placeholder would interpolate in
                     the application that declares this parent, not here. Kept equal to the
                     version above by the release tooling and the post-release bump. -->
                <version>0.3.0-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.6.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <!-- Kept in lockstep with the framework root POM's pin below 3.6.0 (see there
                         for the archetype skip-regression this avoids). -->
                    <version>3.5.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.google.dagger</groupId>
                            <artifactId>dagger-compiler</artifactId>
                        </path>
                        <path>
                            <groupId>dev.vertique</groupId>
                            <artifactId>vertique-codegen-all</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
