<?xml version="1.0" encoding="UTF-8"?>
<!--
SPDX-FileCopyrightText: 2026 Koivisto Capital Oy
SPDX-License-Identifier: EUPL-1.2
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.vertique</groupId>
    <artifactId>vertique-app-parent</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>
    <name>Vertique Application Parent</name>
    <description>Standalone Maven parent for Vertique applications</description>
    <url>https://vertique.dev</url>

    <licenses>
        <license>
            <name>European Union Public Licence v. 1.2</name>
            <url>https://joinup.ec.europa.eu/collection/eupl/eupl-text-eupl-12</url>
        </license>
    </licenses>

    <organization>
        <name>Koivisto Capital Oy</name>
        <url>https://vertique.dev</url>
    </organization>

    <developers>
        <developer>
            <id>vertique-release</id>
            <name>Vertique Release</name>
            <email>releases@vertique.dev</email>
            <organization>Koivisto Capital Oy</organization>
            <organizationUrl>https://vertique.dev</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/vertiquehq/vertique.git</connection>
        <developerConnection>scm:git:git@github.com:vertiquehq/vertique.git</developerConnection>
        <url>https://github.com/vertiquehq/vertique</url>
    </scm>

    <properties>
        <revision>0.3.0-SNAPSHOT</revision>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.vertique</groupId>
                <artifactId>vertique-bom</artifactId>
                <version>${revision}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.5</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>3.5.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>com.google.dagger</groupId>
                            <artifactId>dagger-compiler</artifactId>
                        </path>
                        <path>
                            <groupId>dev.vertique</groupId>
                            <artifactId>vertique-codegen-all</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <!--
                  Declared so the `spotless` goal prefix resolves for this module when the
                  framework reactor is invoked from the root (`./mvnw spotless:apply`). This POM
                  is deliberately standalone (no parent), so it inherits none of the framework's
                  plugin declarations, and a missing prefix here fails the whole reactor.

                  inherited=false: applications that use this POM as their parent are NOT
                  subjected to the framework's own code-formatting policy.

                  Keep the version in sync with `spotless.version` in the framework root POM.
                -->
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>3.3.0</version>
                <inherited>false</inherited>
                <configuration>
                    <java>
                        <palantirJavaFormat/>
                        <removeUnusedImports/>
                    </java>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.7.0</version>
                <inherited>false</inherited>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten-clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
